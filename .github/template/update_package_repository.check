#!/bin/sh -eu
curl -s --fail --retry 16 --header "Authorization: Bearer $GITHUB_TOKEN" "${GITHUB_API_URL:-https://api.github.com}"/repos/"$GITHUB_REPOSITORY"/releases'?per_page=100' | jq '.[].assets[].name' -r | grep -qE '.deb$|.rpm$'
